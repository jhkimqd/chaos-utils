apiVersion: chaos.polygon.io/v1
kind: ChaosScenario
metadata:
  name: rabbitmq-total-failure
  description: Simulate complete RabbitMQ messaging failure
  tags: [network, dependency, messaging, high-severity]
  author: DevTools
  version: "0.0.1"

spec:
  # Target the RabbitMQ messaging node
  targets:
    - selector:
        type: kurtosis_service
        enclave: "${ENCLAVE_NAME}"
        pattern: "rabbitmq"
      alias: rabbitmq_node

  duration: 3m
  warmup: 30s
  cooldown: 30s

  # Block all RabbitMQ traffic (AMQP and Management)
  faults:
    - phase: total_rabbitmq_failure
      description: Simulate complete RabbitMQ unavailability
      target: rabbitmq_node
      type: network
      params:
        device: eth0
        packet_loss: 100
        target_ports: "5672,15672"
        target_proto: tcp

  success_criteria:
    - name: bor_resilient
      description: Bor continues producing blocks without any 1-minute gap despite messaging issues
      type: prometheus
      query: min(min_over_time(rate(chain_head_block[1m])[3m:1m]))
      threshold: "> 0"
      window: 3m
      critical: true

    - name: heimdall_resilient
      description: Heimdall consensus continues without any 1-minute gap despite messaging issues
      type: prometheus
      query: min(min_over_time(rate(cometbft_consensus_height[1m])[3m:1m]))
      threshold: "> 0"
      window: 3m
      critical: true

    - name: checkpoint_api_working
      description: Checkpoint API may be affected but should still respond
      type: prometheus
      query: min(min_over_time(rate(heimdallv2_checkpoint_api_calls_total[3m])[3m:1m]))
      threshold: "> 0"
      window: 3m
      critical: false

    # ========== SLOW BLOCK & SPAN ROTATION DETECTION ==========

    - name: block_times_acceptable
      description: Block intervals stay within acceptable range — detects slow blocks from degraded p2p or span delays
      type: prometheus
      query: rate(cometbft_consensus_block_interval_seconds_sum{job=~"l2-cl-.*-heimdall-v2-bor-validator"}[2m]) / clamp_min(rate(cometbft_consensus_block_interval_seconds_count{job=~"l2-cl-.*-heimdall-v2-bor-validator"}[2m]), 0.001)
      threshold: "< 10.0"
      window: 3m
      critical: false

    - name: span_api_success_rate
      description: Span API call success rate remains healthy — detects span rotation issues from p2p or network delays
      type: prometheus
      query: (sum(rate(heimdallv2_bor_api_calls_success_total[5m])) / clamp_min(sum(rate(heimdallv2_bor_api_calls_total[5m])), 0.001)) or vector(1)
      threshold: ">= 0.8"
      window: 3m
      critical: false

    - name: bor_span_requests_healthy
      description: Bor-side span fetch requests succeed — detects client-side span rotation failures from p2p delays or Heimdall unavailability
      type: prometheus
      query: (sum(rate(client_requests_span_valid[5m])) / clamp_min(sum(rate(client_requests_span_valid[5m])) + sum(rate(client_requests_span_invalid[5m])), 0.001)) or vector(1)
      threshold: ">= 0.8"
      window: 3m
      critical: false


  metrics:
    - chain_head_block
    - cometbft_consensus_height
    - heimdallv2_checkpoint_api_calls_total
    - cometbft_consensus_validators
    # Slow block & span rotation detection
    - cometbft_consensus_block_interval_seconds_sum
    - cometbft_consensus_block_interval_seconds_count
    - heimdallv2_bor_api_calls_total
    - heimdallv2_bor_api_calls_success_total
    # Bor-side span rotation detection
    - client_requests_span_valid
    - client_requests_span_invalid
    - client_requests_span_duration
    - client_requests_latestspan_valid
    - client_requests_latestspan_invalid
